import React from 'react';

const Book = () => (
  <section>
    <div>
      <h1>Book Title</h1>
      <p>Book Author</p>
    </div>
    <button type="button">Remove</button>
  </section>
);

export default Book;
